# ARGUEMENT IN PYTHON:
- an arguement also called as parameter
- it is the value passed to funtion

# TYPES OF ARGUEMENTS:
- Positional Argument
- Default Argument
- Arbitrary Argument
- Arbitrary Keyword Argument
- Mixed Argument

# ARGUMENT PACKING:
- Arguments passed to the parameter and then it is packed as tuple,list and dictionary

# UNPACKING ARGUMENT:
- 
# LAMBDA FUNCTION IN PYTHON:
- a lambda function is a small anonymous function in python

# WHERE IT IS USED?
 - inside the map function 
 - filter function [ map()]
 - reduce function [reduce()]
 - sorted function [sorted()] // mostly used

 # TYPES OF LAMBDA FUNCTIONS :
- lambda with no parameter
- lambda with one parameter
- lambda with multiple parameter 
- lambda with conditional expression [ also called as lambda expression]
- nested lambda 


